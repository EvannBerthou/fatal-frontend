image: node:latest

stages:
  - install
  - build
  - copy

cache:
  paths:
    - node_modules/

install:
  stage: install
  script:
    - npm install

build:
  stage: build
  script:
    - npm run build

production:
  stage: copy
  script:
    - ls -la